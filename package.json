{
  "name": "dayofweek",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . DayOfWeekApp --platform=win32 --arch=x64 --overwrite --icon=assets/icon.ico && explorer DayOfWeekApp-win32-x64"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^34.0.1"
  }
}
